generate:
  build:
    image: generate
    dockerfile_path: Dockerfile.generate
  volumes:
    - ./_site:/site
deploy:
  build:
    image: deploy
    dockerfile_path: Dockerfile.deploy
  volumes:
    - ./_site:/site
  encrypted_env_file: github-oauth-token.env.encrypted
